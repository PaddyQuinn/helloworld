<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @list.name %>
</p>

<p>
  <strong>Text:</strong>
  <%= @list.desc %>
</p>

<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Short Stories', lists_path %>

<%= render 'footer' %>
